name: Windows Tauri Build

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    paths:
      - components.json
      - index.html
      - package.json
      - package-lock.json
      - tsconfig.json
      - tsconfig.node.json
      - vite.config.ts
      - vitest.config.ts
      - tooling/**
      - src/**
      - src-tauri/**
      - tests/frontend/**
      - .github/workflows/windows-tauri-release.yml
  push:
    branches:
      - "**"
    tags:
      - "v*"
    paths:
      - components.json
      - index.html
      - package.json
      - package-lock.json
      - tsconfig.json
      - tsconfig.node.json
      - vite.config.ts
      - vitest.config.ts
      - tooling/**
      - src/**
      - src-tauri/**
      - tests/frontend/**
      - .github/workflows/windows-tauri-release.yml

permissions:
  contents: read

concurrency:
  group: windows-tauri-${{ github.workflow }}-${{ github.event.pull_request.head.sha || github.sha }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: 0

jobs:
  resolve-release-meta:
    name: Resolve Release Metadata
    runs-on: ubuntu-latest
    timeout-minutes: 10

    outputs:
      release_requested: ${{ steps.release_meta.outputs.release_requested }}
      release_tag: ${{ steps.release_meta.outputs.release_tag }}
      release_version: ${{ steps.release_meta.outputs.release_version }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Resolve release metadata
        id: release_meta
        shell: bash
        run: |
          package_version="$(node -p "require('./package.json').version")"
          release_requested="false"
          release_tag="v${package_version}"
          release_version="${package_version}"

          if [[ "${GITHUB_REF}" == refs/tags/v* ]]; then
            release_requested="true"
            release_tag="${GITHUB_REF_NAME}"

            if [[ "${release_tag}" != "v${package_version}" ]]; then
              echo "标签 ${release_tag} 与 package.json 版本 v${package_version} 不一致。" >&2
              exit 1
            fi
          fi

          {
            echo "release_requested=${release_requested}"
            echo "release_tag=${release_tag}"
            echo "release_version=${release_version}"
          } >> "$GITHUB_OUTPUT"

  verify-frontend:
    name: Verify Frontend
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install frontend dependencies
        run: npm ci --prefer-offline --no-audit --fund=false

      - name: Run sensitive path checks
        run: npm run check:sensitive-paths

      - name: Run frontend tests
        run: npm run test

      - name: Run TypeScript checks
        run: npm run typecheck

      - name: Build frontend dist
        run: npm run build:dist

      - name: Upload frontend dist artifact
        uses: actions/upload-artifact@v6
        with:
          compression-level: 0
          name: codex-switch-frontend-dist
          if-no-files-found: error
          path: dist

  verify-rust:
    name: Verify Rust Backend
    runs-on: windows-latest
    timeout-minutes: 25

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 20

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust build output
        uses: swatinem/rust-cache@v2
        with:
          shared-key: windows-tauri
          workspaces: |
            src-tauri -> target

      - name: Run Rust tests
        run: npm run test:rust

  build-windows:
    name: Build Windows Installer
    needs:
      - verify-frontend
      - verify-rust
    runs-on: windows-latest
    timeout-minutes: 45

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust build output
        uses: swatinem/rust-cache@v2
        with:
          shared-key: windows-tauri
          workspaces: |
            src-tauri -> target

      - name: Download frontend dist artifact
        uses: actions/download-artifact@v5
        with:
          name: codex-switch-frontend-dist
          path: .

      - name: Install frontend dependencies
        run: npm ci --prefer-offline --no-audit --fund=false

      - name: Build Windows bundles
        env:
          SKIP_TAURI_FRONTEND_BUILD: "1"
        run: npm run build:windows

      - name: Upload Windows bundle artifacts
        uses: actions/upload-artifact@v6
        with:
          compression-level: 0
          name: codex-switch-windows-bundles
          if-no-files-found: error
          path: |
            src-tauri/target/release/bundle/nsis/*.exe

  publish-release:
    name: Publish GitHub Release
    needs:
      - resolve-release-meta
      - build-windows
    if: needs.resolve-release-meta.outputs.release_requested == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: write

    steps:
      - name: Download Windows bundle artifacts
        uses: actions/download-artifact@v5
        with:
          name: codex-switch-windows-bundles
          path: release-artifacts

      - name: Create GitHub release
        env:
          GITHUB_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          tag="${{ needs.resolve-release-meta.outputs.release_tag }}"
          version="${{ needs.resolve-release-meta.outputs.release_version }}"
          repo="${{ github.repository }}"

          if gh release view "$tag" --repo "$repo" >/dev/null 2>&1; then
            gh release upload "$tag" release-artifacts/*.exe --repo "$repo" --clobber
          else
            gh release create "$tag" release-artifacts/*.exe --repo "$repo" --verify-tag --title "Codex Switch $version" --generate-notes
          fi